const expect = require('chai').expect;
const StreamMethods = require("../components/readStreams");

describe('Suite testing the requests for streams', () => {
    it('should return the request stream range', () => {
        
    })
    it('should return the whole file when end bytes are omited', () => {

    })
    it('should return a 400 status request when range omited', () => {

    })
})
